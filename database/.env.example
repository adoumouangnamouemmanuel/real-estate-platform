# database/.env.example
# Copy to .env and fill in real values for local development.
# Never commit .env — it is already in .gitignore.

DATABASE_URL="postgresql://lumavok_user:password@localhost:5432/lumavok"
